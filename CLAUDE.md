see AGENTS.md

# Builds

Run any build invocations (e.g. `gradlew assembleDebug`, `gradlew build`) via a Haiku subagent rather than inline. Use `Agent(subagent_type="general-purpose", model="haiku", ...)` for build tasks.
